name: Build users service

on:
  push:
    branches:
      - main
    paths:
      - 'backend/services/users/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend/services/users
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.5'
          cache-dependency-path: backend/services/users/go.sum

      - name: Download dependencies
        run: go mod download

      - name: Build
        run: go build -o users-server ./cmd/server

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: users-server
          path: backend/services/users/users-server
          retention-days: 14
